//策略分析 strategry.call(analysis);
var analysis = function(){

    // kdj.d大于90的后续涨幅
    this.between = function (startDate, endDate) {
        var first = data[0], maxK = first[kk], maxD = first[kk], maxJ = first[kk], temp = [];
        for (var i = 0, l = data.length; i < l; i++) {
            var e = data[i];

        }

    };


    //后续第X天的平均涨幅最高

};